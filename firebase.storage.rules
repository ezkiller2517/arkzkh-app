rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all files. This allows anyone with the URL
    // to view the files, which is common for web app assets.
    match /{allPaths=**} {
      allow read;
      allow write: if request.auth != null;
    }
  }
}
