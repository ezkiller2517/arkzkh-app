build:
  steps:
    - name: fetch
      fetch:
        directory: .
    - name: npm-install
      npm:
        install:
          directory: .
    - name: npm-build
      npm:
        run:
          script: build
          directory: .
    - name: npm-start
      npm:
        run:
          script: start
          directory: .

runConfig:
  runtime: nodejs20
  workingDirectory: .
  env:
    - key: PORT
      value: "${PORT}"
