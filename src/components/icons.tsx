import { Compass } from 'lucide-react';

export const Icons = {
  Logo: () => (
    <div className="flex items-center justify-center gap-2 font-headline text-lg font-bold text-primary">
      <Compass className="h-6 w-6" />
      <span>AlignmentOS</span>
    </div>
  ),
};
